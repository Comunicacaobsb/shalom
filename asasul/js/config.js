/* =====================================================================
   CONFIGURAÇÃO DO SUPABASE
   ---------------------------------------------------------------------
   Preencha com os dados do seu projeto (Supabase → Project Settings → API).
   - url:     "Project URL"      (ex.: https://xxxxxxxx.supabase.co)
   - anonKey: "anon public" key  (pode ficar exposta — é pública por design;
              quem protege a escrita é o RLS configurado no supabase-setup.sql)

   Enquanto estiver com os valores de exemplo abaixo, o site continua
   funcionando com os dados estáticos de js/data.js (modo de demonstração).
   ===================================================================== */
window.SHALOM_SUPABASE = {
  url:     "COLE_AQUI_A_PROJECT_URL",
  anonKey: "COLE_AQUI_A_ANON_KEY"
};
